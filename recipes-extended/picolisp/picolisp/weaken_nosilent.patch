--- pil21/src/Makefile	2025-09-09 10:18:26.267518538 +0200
+++ pil21/src/Makefile	2025-09-09 10:29:23.310604570 +0200
@@ -1,22 +1,17 @@
 # 06sep25 Software Lab. Alexander Burger
 
-.SILENT:
 
-CC = clang
+CC ?= clang
 PIL = ../pil  # pil
-ASM = opt -O2  # llvm-as
-LLC = llc
-LINK = llvm-link
+ASM ?= opt -O2  # llvm-as
+LLC ?= llc
+LINK ?= llvm-link
 SHARED = -shared
-STRIP = strip
+STRIP ?= strip
+LLVM_CONFIG ?= llvm-config
 
-LLVM = $(shell llvm-config --version | cut -d. -f1)
-ifeq ($(shell test $(LLVM) -ge 15 -a $(LLVM) -lt 17; echo $$?), 0)
-	ASM += -opaque-pointers
-endif
-
-OS = $(shell uname)
-CPU = $(shell uname -m)
+OS ?= $(shell uname)
+CPU ?= $(shell uname -m)
 
 BIN = ../bin
 LIB = ../lib
@@ -29,11 +24,11 @@
 
 # System definitions
 $(LIB)/sysdefs: sysdefs.c
-	$(CC) -w -D_OS='"$(OS)"' -D_CPU='"$(CPU)"' sysdefs.c  &&  ./a.out > $(LIB)/sysdefs  &&  rm ./a.out
+	$(CC) -w -D_OS='"$(OS)"' -D_CPU='"$(CPU)"' sysdefs.c -o sysdef
 
 # Base system
 $(BIN)/picolisp: picolisp.bc
-	mkdir -p $(BIN)
+	mkdir -p $(BIN) $(LIB)
 	$(LLC) picolisp.bc -relocation-model=pic -o picolisp.s
 	$(CC) picolisp.s -o $(BIN)/picolisp -rdynamic -lc -lutil -lm -ldl `pkg-config --libs readline libffi`
 	$(STRIP) $(BIN)/picolisp
